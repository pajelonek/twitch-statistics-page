export const config = {
    maxRequests: 10, // Maximum number of requests to be made during one batch
    language: 'pl', // Language filter for streams
    first: 100, // Number of streams to fetch per request
};