{
  "name": "async-statistics-calc",
  "version": "1.0.0",
  "description": "A Node.js application that runs periodically to calculate statistics for twitch-statistics-page",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "copy-files": "mkdir -p dist/test_data && cp -r test_data/* dist/test_data/",
    "copy-files-win": "if not exist dist\\test_data mkdir dist\\test_data && xcopy /E /I test_data dist\\test_data",
    "start": "tsc &&  node dist/index.js"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^22.2.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.4"
  },
  "dependencies": {
    "axios": "^1.7.4",
    "dotenv": "^16.4.5"
  }
}
